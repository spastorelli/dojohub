### Build the library

1. Install dependencies:
  ```bash
  npm install
  ```
2. Build the library bundle:
  ```bash
  gulp build
  ```
  or build the minified bundle:
  ```bash
  gulp build:min
  ```
